import { describe, expect, it } from 'vitest';
import IconStack from './IconStack.svelte';

describe('IconStack', () => {
	it('exports a Svelte component', () => {
		expect(IconStack).toBeTruthy();
	});
});
